package interface1;

public interface Example
//If We are using any method inside the interface by default it is abstract method.
{
	 void display();//abstract method cannot have any body.
	//void print();//we can use 2 methods inside the interface
}
